{
  "name": "@eotickets/common",
  "version": "1.0.1",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "license": "MIT",
  "scripts": {
    "clean": "del ./build/*",
    "build": "yarn clean && tsc",
    "publish-all": "git add . && git commit -m \"Updates\" && npm version patch && yarn build && npm publish"
  },
  "devDependencies": {
    "del-cli": "^5.0.0",
    "typescript": "^4.8.2"
  }
}
